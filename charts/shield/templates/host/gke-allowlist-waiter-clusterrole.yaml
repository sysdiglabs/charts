{{- if (include "host.allowlist_waiter.enabled" .) -}}
{{- if (include "host.allowlist_waiter.rbac.cluster_role.create" .) }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "host.allowlist_waiter.fullname" . }}
  labels:
    {{- include "host.rbac_labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: "pre-install,pre-upgrade"
    helm.sh/hook-weight: "-5"
    helm.sh/hook-delete-policy: "before-hook-creation"
    {{- $rbacAnnotations := (include "host.rbac_annotations" .) }}
    {{- if $rbacAnnotations }}
    {{- $rbacAnnotations | nindent 4 }}
    {{- end }}
rules:
  - apiGroups:
      - auto.gke.io
    resources:
      - allowlistsynchronizers
    verbs:
      - get
      - list
      - watch
{{- end }}
{{- end -}}
