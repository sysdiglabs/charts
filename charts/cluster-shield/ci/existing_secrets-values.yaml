---
global:
  sysdig:
    accessKeySecret: "a-fake-secret-name"
cluster_shield:
  cluster_config:
    name: "lint-cluster"
  sysdig_endpoint:
    api_url: "https://www.example.org"
    collector: "example.com:6443"
