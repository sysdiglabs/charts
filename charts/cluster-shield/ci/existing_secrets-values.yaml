---
global:
  sysdig:
    existingAccessKeySecret: "a-fake-secret-name"
    existingSecureApiTokenSecret: "another-fake-secr"
cluster_shield:
  cluster_config:
    name: "lint-cluster"
  sysdig_endpoint:
    api_url: "https://www.example.org"
    collector: "example.com:6443"
