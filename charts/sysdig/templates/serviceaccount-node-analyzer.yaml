{{- if .Values.serviceAccount.nodeAnalyzer.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "sysdig.nodeAnalyzerServiceAccountName" .}}
  labels:
  {{ include "sysdig.labels" . | indent 4 }}
  {{- end }}
