suite: Agent Events Tests
templates:
  - templates/clusterrole.yaml
tests:
  - it: Test events information included in k8s
    asserts:
      - contains:
          path: rules
          content:
            apiGroups:
              - ""
            resources:
              - "events"
            verbs:
              - "create"
              - "patch"

