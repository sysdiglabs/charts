suite: Test admissioncontrollerconfigmap
templates:
  - webhook/admissioncontrollerconfigmap.yaml
tests:
  - it: Creates the configmap if webhook.acConfig is present
    set:
      webhook:
        acConfig: |
          foo: bar
          fizz: buzz
    asserts:
      - containsDocument:
          kind: ConfigMap
          apiVersion: v1
          template: webhook/admissioncontrollerconfigmap.yaml
#  - it: Does not create the configmap if webhook.acConfig is not present
#    asserts:
#      - containsDocument:
#          kind: ConfigMap
#          apiVersion: v1
#          template: webhook/admissioncontrollerconfigmap.yaml
#        not: true
