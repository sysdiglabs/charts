apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "admissionController.webhook.fullname" . }}
spec:
  maxReplicas: {{ .Values.admissionController.webhook.autoscaling.maxReplicas }}
  minReplicas: {{ .Values.admissionController.webhook.autoscaling.minReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "admissionController.webhook.fullname" . }}
  targetCPUUtilizationPercentage: {{ .Values.admissionController.webhook.autoscaling.targetCPUUtilizationPercentage }}
